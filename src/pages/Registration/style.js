import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titulo:{
        textAlign: 'center',
        marginTop: 20,
        fontSize: 20,
        fontWeight: 'bold',
    }
})

export default styles