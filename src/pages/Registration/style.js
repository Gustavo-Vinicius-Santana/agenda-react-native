import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titulo:{
        textAlign: 'center',
        marginTop: 20,
        fontSize: 20,
        fontWeight: 'bold',
    },
    boxRegister:{
        marginTop: 15,
        width:'100%',
        alignItems:'center',
    }
})

export default styles