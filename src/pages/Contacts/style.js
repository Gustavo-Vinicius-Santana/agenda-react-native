import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxContatos:{
        width: '100%',
        alignItems:"center",
    },boxItem:{
        marginTop: 15,
        marginBottom: 15,
        width: '50%',
    },clickInfo:{
        marginTop:4,
        fontSize: 15,
        color: "#ff0033",
        fontWeight: "bold",
        textAlign:'center',
    }
})

export default styles